# deno-tRPC

## Overview
Build a Typesafe API with Deno tRPC

 ## Details

 Install dependencies:
 ```bash
  deno install
 ```

 Start client:
  ```bash
  deno task client
 ```

Start server:
  ```bash
  deno task server
 ```
